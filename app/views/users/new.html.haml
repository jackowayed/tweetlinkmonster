%h1 Users controller, new action

%p 
  Edit this file in 
  %tt app/views/users/new.html.erb

= error_messages_for :user

- form_for(@user, :action => url(:users) ) do |f|
  %p
    = submit_button "Create"

= link_to 'Back', url(:users)